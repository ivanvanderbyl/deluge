{{yield (hash
  item=(component "deluge-item"
        menu=this
        isDisabled=this.isDisabled
        action=(action "itemSelected")
        on-add=(action "addItem")
        on-remove=(action "removeItem"))
)}}
