{{yield (hash
  width=width
  height=height
  item=(component "deluge-item"
        menu=this
        disabled=this.disabled
        action=(action "itemSelected")
        add=(action "addItem")
        remove=(action "removeItem"))
)}}
